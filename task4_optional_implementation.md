# Task 4 — Optional Basic Implementation

This task is optional.

Complete your solution in:
- `answers/task4_optional_solution.py`
- `answers/task4_optional_notes.md`

## Problem

Write a Python function:

```python
def find_duplicate_booking_ids(booking_ids: list[str]) -> list[str]:
    ...
```

The function should return a list of duplicate booking IDs in the order they are first detected.

### Example
Input:
```python
["b1", "b2", "b3", "b2", "b4", "b1", "b1"]
```

Output:
```python
["b2", "b1"]
```

## Requirements
- Do not use external libraries.
- Keep the implementation simple and readable.
- Briefly explain your approach in `answers/task4_optional_notes.md`.
